# Python Environment Rules

This project uses uv-managed virtual environments.

Always use the local virtual environment interpreter located at:

.venv/bin/python

Do not use:
- system Python
- pyenv Python
- global pip
- poetry environments
- conda environments

When running Python commands:
- use `.venv/bin/python`
- use `.venv/bin/pip`
- prefer `uv run` where appropriate

Examples:
- `.venv/bin/python script.py`
- `.venv/bin/python -m pytest`
- `.venv/bin/pip install package`
- `uv run python script.py`

Assume dependencies are installed into the local `.venv`.

If the environment does not exist, create it with:

```bash
uv venv

# Other rules

Do not read anything in the directory `archive`.

Check the number of lines before reading tabular data files (e.g. *.csv). If the file has more than 50 lines, ask permission to read it. 

Do not attempt to use replace_in_file in PLAN MODE. Ask to switch to ACT MODE.

Be careful not to loop over the same thought patters over and over again. 

When replace_in_file tool fails, and then you are given a file_content block, try using replace_in_file tool again against that content, instead of content returned in replace_in_file.

When working with Jupyter notebook file, do not try to write the complete file content on failure. Instead, when given a file_content block, try using replace_in_file tool again with that content in mind.